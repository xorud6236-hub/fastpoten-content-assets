# 다음 작업 브리프 (2026-07-07, 사용자 피드백 반영)

> 세션 재시작 후 이 브리프부터 읽고 이어간다. 근거: 사용자 피드백(FEEDBACK_LOG 2026-07-07).

## 받은 피드백
1. **CA-3 시연** — 합성 샘플이라 실감이 덜함. **실제 작성됐던 글**로 보면 더 정확히 판단 가능.
2. **CB-1 카드** — 방향은 채택(나쁘지 않음). 다만 **퀄리티가 아직 만족스럽지 않아 진행하며 고도화** 필요.

## 준비된 다음 작업 (사용자 복귀 시 택1·병행 가능)

### A. CA-3 실제 글 재검증 — *사용자 입력 필요*
- 현 DB엔 카페 글 **메타만** 있고 본문 텍스트는 없음(자동 추출=백로그). 실제 본문 1건이 있어야 검증됨.
- 방법: `inbox/<슬러그>/body.txt`(글 전문) + `meta.json` → `python src/intake_manual.py inbox/<슬러그>`.
  - 양식·설명: `examples/intake_sample/_템플릿/README.md`, 완성 예시: `examples/intake_sample/임상심리사2급_응시자격/`.
- 실제 글이면 문단 역할·마스킹·이미지 분류가 실데이터로 검증됨. (역할 태깅은 규칙 기반 1차라 실제 글에선 오태깅이 더 보일 수 있음 → 그 결과가 AI 태깅 도입 판단 근거가 됨.)

### B. 카드뉴스 품질 고도화 (트랙 B) — *결정 1건 필요: 브랜드 색/로고*
현재 한계: 정보 정렬은 깔끔하나 밋밋함(브랜드 개성·시각 리듬 부족).
고도화 방향(제안):
- 각 조건 항목에 **아이콘/픽토그램** 부여(텍스트 나열 탈피).
- 타입 스케일·색 깊이 강화, 포인트 컬러 1곳 집중, 여백 리듬 정리.
- 절차 카드엔 **단계 번호 뱃지**, 비교 카드엔 좌우 대비 강조.
- `templates/brand.css` 색을 **실제 패스트포텐 브랜드 색/로고**로 교체.
- **복귀 시 물어볼 것(일상 언어)**: "브랜드 대표 색(로고 색)이 있나요? 로고 이미지 파일이 있으면 카드에 넣을게요."
- 이후 CB-2에서 절차·비교 2종을 이 상향된 스타일로 추가.

### C. CA-4 린터 — *결정 불요, 즉시 착수 가능*
- 금지어(14) · 단정 수치(학점은행제 기간·비용 등) · 개인정보(`masking.py` 재사용) · 채널별 길이 검사 + 리포트.
- 승인 큐·생성 단계 검수의 기반. `masking.py`가 이미 있어 착수 즉시 가능(동기화 필수 쌍 자동 충족).

## 추천 순서
1. 복귀하면 **브랜드 색 질문 1개**만 받기 → 답 오면 카드 고도화(B) 착수.
2. 병행으로 **CA-4 린터(C)** 는 결정 불요라 바로 시작 가능.
3. 사용자가 **실제 글**을 inbox에 넣어주면 CA-3 재검증(A).
