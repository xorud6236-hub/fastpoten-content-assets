# BACKLOG — 요청 시 착수

1. **Playwright 자동 추출기 — 카페+블로그 공용**: 기존 순위체크 방식 확장(가상 브라우저→본문+이미지). 블로그는 전체공개라 카페보다 쉬움. 요건: 로그인 세션(카페), 속도 제한, 접근상태 분류, 체크포인트.
2. 인스타 직접 연동(기성품으로 부족해질 때).
3. 직원용 기능(프롬프트 팩 / 표현 뱅크 / 검수기 공유) — 기준 사용자 재정의로 강등. 직원 확산 시 재개.
4. 네이버 채널용 완성 원고 출력기(발행은 사람).
5. 웹 뷰어 / 글 종류(use_case) 분류 / 골든샘플 / CRM 통합(맨 마지막).
6. **staff 이름 목록 정제** — `ingest_excel.py`가 검증 없이 넣어 82건에 이름 아닌 값이 섞임(숫자 1글자, 한 글자 조각, 점이 앞에 붙은 이름, 업체명, 공백 때문에 7글자로 잡힌 중복 이름). 지금은 무해(2자 미만 제외 + 오탐 0)지만, 일반 단어와 겹치는 2자 이름이 들어오면 마스킹 오탐이 정제·린터·뷰어에 동시에 번진다(동기화 필수 쌍). 실제 값은 `python -c` 로 staff 테이블을 직접 볼 것 — **이 저장소는 공개이므로 실명·실제 값을 문서에 옮겨 적지 말 것**(불변 1). 근거: FEEDBACK_LOG 2026-07-15.
