# BACKLOG — 요청 시 착수

1. **Playwright 자동 추출기 — 카페+블로그 공용**: 기존 순위체크 방식 확장(가상 브라우저→본문+이미지). 블로그는 전체공개라 카페보다 쉬움. 요건: 로그인 세션(카페), 속도 제한, 접근상태 분류, 체크포인트.
2. 인스타 직접 연동(기성품으로 부족해질 때).
3. 직원용 기능(프롬프트 팩 / 표현 뱅크 / 검수기 공유) — 기준 사용자 재정의로 강등. 직원 확산 시 재개.
4. 네이버 채널용 완성 원고 출력기(발행은 사람).
5. 웹 뷰어 / 글 종류(use_case) 분류 / 골든샘플 / CRM 통합(맨 마지막).
6. **✅ staff 이름 목록 정제 — 완료(2026-07-21)**. 실행: `src/staff_cleanup.py`(드라이런 기본, `--apply`로 실행). 결과: staff 82→78 · posts.staff_name 11건 정정 · **훼손됐던 글 15건 복구**(매경테스트·레벨테스트 등이 다시 보임) · 실명 노출 0 · 글 건수 전부 불변 · 테스트 228개 통과. 되돌리기 기록 `data/staff_cleanup_20260721.json`, 창고 사본 `Documents\fastpoten-backup\…_before_staff_cleanup.sqlite3`.
   - **★ 드라이런이 사고를 막았다**: 지우려던 값 하나(같은 사람의 다른 철자)가 **본문에 실제 사람 이름으로 쓰인 곳이 3건** 있었다("평생교육 멘토 OOO입니다"). 그대로 지웠으면 참고용 본문에 실명이 드러났다(불변 1 위반). → **'화면 고르는 칸'과 '가리는 재료'를 분리**: posts.staff_name은 올바른 이름으로 정정하고, staff 행은 가림 재료로 남겼다. 고르는 칸은 posts에서 뽑으므로 목록은 깨끗해진다.
   - **남은 것**: 재발 방지(`ingest_excel.py`가 담당자 칸을 검증 없이 넣는 것)는 **안 고쳤다** — 아래 6-1.
   - <details><summary>당시 기록(펼치기)</summary>

   **★ staff 이름 목록 정제 — 이미 오탐이 나고 있음(사용자 확인 후 결정 대기)**
   - **실증된 피해**: 이름 목록 82건에 **`테스트`**(사람 이름이 아니라 일반 단어)가 들어 있어 마스킹이 정상 문장을 망가뜨린다. 예: "테스트 응시 자격은…" → "**[담당자]** 응시 자격은…", "모의 테스트를…" → "모의 **[담당자]**를…". **추출된 글 3,621건 중 15건**의 참고용 본문이 실제로 훼손됨(2026-07-15 실측).
   - **원문은 안전**: 불변 4(원문 절대 수정 금지) 덕에 body_raw는 그대로 → 목록을 고치고 참고용 본문을 다시 만들면 **복구된다**(영구 손실 아님). 불변 4가 안전망으로 작동한 사례.
   - **팩트(CA-6)는 다르다**: 적재기가 추가 전용이라 잘못 가려진 채 들어가면 **재적재로 못 고친다**(4차 수정 화면이 생기기 전까지). 현재 룰북에 "테스트"라는 단어가 없어 살았을 뿐이다.
   - 그 밖의 이름 아닌 값: 숫자 1글자, 한 글자 조각, 점이 앞에 붙은 이름, 업체명, 공백 탓에 7글자로 잡힌 중복 이름. (1글자는 마스킹이 2자 미만을 걸러 무해.)
   - 원인: `ingest_excel.py`가 '담당자 칸/날짜 다음 칸' 문자열을 검증 없이 `INSERT OR IGNORE`.
   - 고칠 때 주의: 가림 규칙은 **동기화 필수 쌍**(린터↔정제)이라 한 곳을 고치면 기존 글 정제·뷰어 왼쪽 화면에 동시에 영향.
   - **실명·실제 값을 이 문서에 옮겨 적지 말 것**(공개 저장소, 불변 1). 값은 staff 테이블을 직접 조회.
   - **2026-07-20 — 우선순위 올라감. 이제 눈에 보인다**: 글 목록에 '담당자로 걸러 보기' 칸이 생기면서 이 목록이 **사용자가 매번 여는 고르는 칸**에 그대로 나온다. 지저분한 값(일반 단어·숫자·점 붙은 이름·업체명·오타 중복·띄어쓰기만 다른 중복)이 실제 직원 이름과 섞여 보여 고르기가 불편하다. 그동안은 안 보이는 곳에 있어 미룰 수 있었으나 이제 아니다.
   - **사용자만 판정 가능**: 어느 값이 실제 직원이고 어느 게 잘못 들어온 것인지는 부서를 아는 사람만 안다. 고치는 방법은 준비돼 있다(정제 후 참고용 본문 재생성 → 훼손 15건 복구).
   - 근거: FEEDBACK_LOG 2026-07-15 · 2026-07-20.
   </details>

6-1. **담당자 칸 검증 — 재발 방지(미착수)**. `src/ingest_excel.py`가 '담당자 칸' 문자열을 검증 없이 `INSERT OR IGNORE` 하는 게 6번의 원인이었다. 지금은 엑셀 재적재를 상시로 하지 않아 급하지 않다. 고치려면 **"무엇을 사람 이름으로 볼 것인가"를 사용자와 먼저 정해야 한다** — 업체명(에듀라이크)은 정당한 값이고, 1글자·숫자·기호 시작은 아니다. 별도 차수.
